## 1. Core Implementation

- [x] 1.1 Update `SettingSaleLocation` model to assign `position` on the `creating` event if null.

## 2. Testing

- [x] 2.1 Manually verify `/sales-location-configurations` allows enabling locations without a SQL error.
- [x] 2.2 Run unit tests for Settings / Locations to ensure existing tests pass.
