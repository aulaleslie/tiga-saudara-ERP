## 1. Resolve the POS transaction code before posting

- [x] 1.1 Add an optional `?string $presetCode` parameter to `PosTransactionService::completeFromCartSnapshot`, used in place of `$this->codeGenerator->generate($settingId)` in the transaction-creation branch
- [x] 1.2 Add a code-resolution helper that returns the existing `pos_transactions.code` when `cart_snapshot['active_transaction_id']` refers to a transaction in the current setting, and otherwise generates a new code
- [x] 1.3 In `FinalizePosCheckoutService::finalize`, resolve the transaction code before `postingAdapter->post()` (L957) and pass it into the adapter context
- [x] 1.4 Pass the same resolved code into `completeFromCartSnapshot` (L1137) so the note and the persisted transaction cannot diverge
- [x] 1.5 Verify the resolved code is threaded through both inline and split posting paths (split delegates per group via `inlinePostingAdapter->post`)

## 2. Write the transaction code into Sale notes

- [x] 2.1 Replace the `POS checkout #<checkoutId>` provenance line in `InlinePosCheckoutPostingAdapter` (L121-125) with a line carrying the resolved POS transaction code
- [x] 2.2 Preserve the existing "no blank suffix when the cashier note is absent" behaviour
- [x] 2.3 Confirm `SplitPosCheckoutPostingAdapter` propagates the code to every group so all generated Sales share one provenance value
- [x] 2.4 Handle the case where no transaction code could be resolved, so posting never writes a malformed provenance line

## 3. Tests for code resolution and note composition

- [x] 3.1 Test: walk-up checkout with no saved draft generates a code and persists it on the completed transaction
- [x] 3.2 Test: checkout of a loaded draft reuses the draft's existing code and does not mint a new one
- [x] 3.3 Test: the code in every generated Sale note matches the completed transaction's code
- [x] 3.4 Test: split checkout writes the same code to all generated Sales
- [x] 3.5 Test: checkout without a cashier note writes the provenance line with no trailing blank line
- [x] 3.6 Test: no generated Sale note contains the internal checkout primary key as its provenance identifier
- [x] 3.7 Update the existing assertions in `POSCheckoutSplitPostingTest` and `POSCheckoutNoteAndPaymentImageTest` that expect the old `POS checkout #<id>` format

## 4. Sales list POS column and search

- [x] 4.1 Add a `posCheckoutSale` / `posCheckout` relation path on `Sale` (or an equivalent eager-loadable resolution) reaching `pos_transactions.code` through `pos_checkout_sales`
- [x] 4.2 Eager-load that path in `SaleTable::render` so codes resolve without one query per row
- [x] 4.3 Add a POS column to `resources/views/livewire/sale/sale-table.blade.php` rendering the code, with a neutral placeholder for non-POS and unlinked Sales
- [x] 4.4 Remove the note's role as a de facto POS identifier in the row, now that a real column exists
- [x] 4.5 Extend `SaleTable::searchSubmit`'s query with a `whereHas` over the pivot matching `pos_transactions.code` and `pos_checkouts.receipt_number` (partial match)
- [x] 4.6 Update the search input placeholder to mention POS transaction number

## 5. Tests for the sales list

- [x] 5.1 Test: a POS-originated Sale displays its POS transaction code
- [x] 5.2 Test: a non-POS Sale and a POS Sale with no linked transaction both render the placeholder without error
- [x] 5.3 Test: search by full and partial POS transaction code returns the Sale
- [x] 5.4 Test: search by receipt number returns every Sale generated by that checkout
- [x] 5.5 Test: searching a split checkout's code returns all its generated Sales
- [x] 5.6 Test: existing search fields (reference, customer, product, tax ref, tag) still match
- [x] 5.7 Test: listing a page of POS Sales does not issue per-row queries for the code

## 6. Read-only Sale modal reachable from POS

- [ ] 6.1 Add a checkout-namespaced route `GET /pos/checkouts/{checkout}/sales/{sale}` for the read-only Sale view
- [ ] 6.2 In the controller, authorize POS checkout viewing, then assert the requested `sale_id` is present in that checkout's `pos_checkout_sales` rows; refuse otherwise
- [ ] 6.3 Load the Sale without any current-setting scoping, so cross-setting Sales resolve instead of returning 404
- [ ] 6.4 Build a read-only Sale detail partial with no edit, delete, payment or dispatch actions
- [ ] 6.5 Render linked Sale references on `Modules/Pos/Resources/views/transactions/show.blade.php`, listing every Sale generated by the transaction's checkout
- [ ] 6.6 Wire the references to open the modal in place without navigating to the Sales module
- [ ] 6.7 Handle transactions with no completed checkout or no generated Sales with a plain statement rather than an empty control

## 7. Tests for the linkage and modal authorization

- [ ] 7.1 Test: a Sale generated in a different setting than the viewer's current setting opens successfully
- [ ] 7.2 Test: pairing a checkout with a Sale it did not generate is refused
- [ ] 7.3 Test: a user without POS checkout view permission is refused
- [ ] 7.4 Test: a split transaction lists all of its generated Sale references
- [ ] 7.5 Test: a draft transaction reports that no Sale documents exist yet
- [ ] 7.6 Test: the modal response exposes no mutating action

## 8. POS transaction detail note display

- [ ] 8.1 Add a note section to `Modules/Pos/Resources/views/transactions/show.blade.php` rendering `PosTransaction.note` raw, with no provenance prefix
- [ ] 8.2 Show a plain "no note" statement when the transaction has none
- [ ] 8.3 Test: a transaction with a note displays it without a provenance prefix; one without a note shows the empty-state text

## 9. Receipt layout

- [ ] 9.1 Replace the `No. Struk` metadata row in `Modules/Pos/Resources/views/receipt.blade.php` with a `No. Transaksi` row carrying the POS transaction code
- [ ] 9.2 Add a small, muted, bottom-corner footer element for the receipt number, styled below `.tail-print-history` (~7px against the footer's 8px)
- [ ] 9.3 Ensure the receipt data payload supplies the transaction code to the view
- [ ] 9.4 Render without error when a checkout has no linked POS transaction
- [ ] 9.5 Test: the metadata block shows the transaction code and no longer shows the receipt number, and the receipt number is still printed in the footer

## 10. Return lookup by transaction code

- [ ] 10.1 Extend `PosReturnLookupService` (L36) to match `pos_transactions.code` in addition to `pos_checkouts.receipt_number`
- [ ] 10.2 Make the receipt-number branch deterministic when more than one checkout shares a receipt number, rather than relying on arbitrary ordering
- [ ] 10.3 Test: lookup succeeds by POS transaction code
- [ ] 10.4 Test: lookup by receipt number still behaves as before
- [ ] 10.5 Test: an identifier matching neither is reported as not found
- [ ] 10.6 Test: an ambiguous receipt number resolves deterministically

## 11. Verification

- [ ] 11.1 Run the POS checkout and note suites (`POSCheckoutNoteAndPaymentImageTest`, `POSCheckoutSplitPostingTest`, `POSCheckoutFinalizeIdempotencyTest`) and confirm the finalize path is unbroken
- [ ] 11.2 Run the POS return suites to confirm lookup changes did not regress the return workflow
- [ ] 11.3 Run the full Sale and Pos module test suites
- [ ] 11.4 Manually verify one inline and one split checkout end to end: note content, sales list column and search, POS detail linkage, cross-setting modal, and receipt layout
