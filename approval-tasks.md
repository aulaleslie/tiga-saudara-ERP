# Approval Tasks

- [x] Add settings columns for return prefixes: create migration for `settings.purchase_return_prefix_document` and `settings.sale_return_prefix_document` (nullable), and update `Modules/Setting/Database/Seeders/SettingDatabaseSeeder.php` plus a backfill seeder (e.g., `PopulateReturnDocumentPrefixSeeder`) to set defaults (`PRRN`, `SLRN`). Verify: existing settings get defaults and new settings can store both prefixes.
- [x] Wire return prefix fields through settings UX + validation: update `Modules/Setting/Resources/views/index.blade.php`, `Modules/Setting/Resources/views/businesses/create.blade.php`, `Modules/Setting/Resources/views/businesses/edit.blade.php`, `Modules/Setting/Http/Requests/StoreSettingsRequest.php`, and `Modules/Setting/Http/Controllers/BusinessController.php`/`SettingController.php`. Verify: forms save both prefixes and validation errors show for over‑length inputs.
- [x] Use new prefixes in return references: update `Modules/PurchasesReturn/Entities/PurchaseReturn.php` and `Modules/SalesReturn/Entities/SaleReturn.php` to build reference IDs using `document_prefix` + return prefix (with `make_reference_id`). Verify: newly created returns use the configured prefixes.

- [x] Add approved-edit/archive permissions to seeders + role UI: extend `Modules/User/Database/Seeders/PermissionsTableSeeder.php` (and decide whether to include in `database/seeders/TenantUserSeeder.php`), then add labels to `Modules/User/Resources/views/roles/create.blade.php` and `Modules/User/Resources/views/roles/edit.blade.php`. Verify: permissions exist after seeding and checkboxes appear in role forms.
- [x] Enforce “goods not processed yet” edit rules with new permissions:
  - Purchases: allow edit/archive when status is `APPROVED` (permission required), but block edits once `RECEIVED_PARTIALLY` or later.
  - Sales: allow edit/archive when status is `APPROVED` (permission required), but block edits once `DISPATCHED`/`DISPATCHED_PARTIALLY`.
  - Purchase returns & sales returns: when `approval_status=approved`, lock supplier/customer fields but allow item edits; once `received`/`dispatched` (or equivalent receive/dispatch milestones) block all edits.
  Update controllers/Livewire components (`Modules/Purchase/Http/Controllers/PurchaseController.php`, `Modules/Sale/Http/Controllers/SaleController.php`, `Modules/PurchasesReturn/Http/Controllers/PurchasesReturnController.php`, `Modules/SalesReturn/Http/Controllers/SalesReturnController.php`, and the return edit Livewire forms) plus any request validation needed to enforce field-level locks. Verify: each rule holds with and without the new permissions.

- [x] Add archiving schema + model helpers: add `archived_at`/`archived_by` columns to purchases, sales, purchase_returns, sale_returns; cast dates + add `scopeArchived`/`scopeActive` helpers. Verify: archived documents can be queried separately and default queries exclude them.
- [x] Implement archive actions + UI labels aligned to “goods not processed yet”: add archive routes/controllers for each document type; show “Archive” instead of “Delete” on approved docs that are still eligible; block delete/archive once goods are processed (purchases received, sales dispatched, returns received/dispatched). Verify: eligible approved docs can be archived with permission, and ineligible docs can neither be edited nor archived.
- [x] Add cancel‑submission flow: allow WAITING_APPROVAL → DRAFTED for purchases/sales and pending → draft for returns; add buttons in list + show pages; update status badges/labels where needed. Verify: a submitted document can be returned to draft and then hard‑deleted.
- [ ] Add archived filters on list pages: Livewire tables (`app/Livewire/Purchase/PurchaseTable.php`, `app/Livewire/Sale/SaleTable.php` + their views) and DataTables (`Modules/PurchasesReturn/DataTables/PurchaseReturnsDataTable.php`, `Modules/SalesReturn/DataTables/SaleReturnsDataTable.php` + index views) should default to active records with a toggle to show archived (including search). Verify: archived docs are hidden by default and visible when the toggle is enabled.

- [ ] Tests/verification: add or update feature tests for (a) approved-edit permission gating by status milestones, (b) archive vs delete rules tied to “goods processed,” (c) cancel submission transitions, (d) return reference prefix generation, (e) return field-level locks (supplier/customer locked on approved but items editable). Verify: tests cover both allowed and denied paths.
