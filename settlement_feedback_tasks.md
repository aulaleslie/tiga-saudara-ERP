# Settlement Feedback Tasks

## Study Notes
- Keep `docs/purchase_settlement_logic.md:1-130` as the requirements source for each settlement method; the numbered sections map cleanly to the controller behaviors we looked at.
- The current implementation of `Modules/PurchasesReturn/Http/Controllers/PurchasesReturnSettlementController.php` already covers much of the execution work: `receiveItemSettlement` handles physical receipt (lines 233-360), helpers `breakStock`/`moveStock` touch damaged/repaired stock (lines 370-533), and `applySettlementEffect` contains the financial flows for `MODIFY_PURCHASE`, `CREDIT`, and `CASH` (lines 538-744). Use these anchors when deciding what still needs polishing.

## 1. Product Repair/Replacement (Per `docs/purchase_settlement_logic.md:5-28`)
[x] 1. Confirm the incoming request still enforces that the item status is `APPROVED_AWAITING_RECEIVE` before touching stock.
[x] 2. Mirror the doc’s input rules inside `receiveItemSettlement`: implemented the same vs. different serial check.
[x] 3. For “repair” (same serial) path: implemented `status = 'AVAILABLE'` and `is_broken = false` restoration.
[x] 4. For “replacement” (different serial) path: implemented **in-place update** of the existing serial record to maintain lineage.
[x] 5. After the serial handling completes, the settlement record is marked as received.

## 2. Return Damaged Goods (Per `docs/purchase_settlement_logic.md:30-44`)
[x] 1. `receiveItemSettlement` already routes into `breakStock` when `method === 'BROKEN_STOCK'` and enforces the `received_quantity` rules (`Modules/...Controller.php:258-344`).  Review the validation rules at lines 258-263 to ensure the expected stock quantities and the `isSerial` special case match the doc’s descriptions for good vs. broken stock.
[x] 2. `breakStock` moves quantities from the source location into `broken_quantity` at the destination (lines 450-533) and increments the product’s global `broken_quantity`.  Add a checklist step: verify that the `ProductSerialNumber` status is set to `BROKEN` for serial-tracked units (`Modules/...Controller.php:333-341`), and that `is_broken = true` + `is_in_return_process = false` at lines 332-341.
[x] 3. Capture any business rules around source status (Good vs. Broken) by confirming whether the controller should inspect the serial’s previous `status` or rely on the incoming settlement method.  If additional metadata is required, plan where to store it before `breakStock` runs.
[x] 4. Finally, after `breakStock` executes, the settlement record is marked as received in the same location block as the repair path (`Modules/...Controller.php:346-359`).  Close the loop by ensuring UI/testing reminders mention checking the resulting `ProductStock` and `Transaction` records created inside `breakStock` (lines 494-532) for accuracy.

## 3. Modify Purchase (Per `docs/purchase_settlement_logic.md:45-103`)
1. The doc splits the flow into target selection (serial vs. non-serial) and execution.  Use `applySettlementEffect`’s `MODIFY_PURCHASE` case (`Modules/...Controller.php:545-611`) as the implementation anchor: it already looks up purchases via serial history (line 563) and falls back to product-based matching when no serial is available (lines 580-587).  Verify this logic satisfies the selection constraints listed in the doc (supplier match, status filters, quantity checks) and add explicit query filters if additional safeguards are needed for statuses like `RECEIVED`/`RECEIVED PARTIALLY`.
2. After determining the purchase, the helper functions `ensurePurchaseDetailHasQuantity`, `reducePurchaseDetailAmounts`, `reduceReceivedNoteDetailQuantity`, and `reducePurchaseDetailCollection` (`Modules/...Controller.php:811-953`) enforce the quantity rules described in the doc (lines 68-101).  Step through these helpers to ensure they reduce both the purchase detail quantities and the linked received note quantities, and add tests for scenarios where serials are mixed with non-serial items.
3. The doc demands recalculation of totals and due amounts once quantities drop.  `recalculatePurchaseTotals` (`Modules/...Controller.php:955-988`) already recomputes subtotal/tax/discount/grand total with the updated details.  Ensure this method is triggered after every modify event (it is invoked at line 589) and that the `due_amount`/`payment_status` in `applySettlementEffect` reflect the doc’s “Unpaid vs. Paid” expectations (lines 70-103).
4. For paid/partial purchases, the controller currently deletes payments, resets `paid_amount` to 0, and marks the status `Unpaid` (lines 591-602).  Confirm this matches the doc (lines 88-103) and consider logging the deleted payments as part of audit trails.  Also add a post-operation check (possibly in `ApplySettlementEffect`) that archives the purchase when all detail quantities reach zero, setting `archived_at`, `archived_by`, and a `status` of `RETURNED` as specified (doc lines 75-102).  The current code recalculates totals but does not archive; this must be added.
5. Stock reduction is already handled by `deductStockForReturn` (`Modules/...Controller.php:750-808`), which updates global + location quantities and handles serial status (lines 773-783).  Include a step in the plan to validate that the serial’s `status` is set to `RETURNED`, `is_in_return_process` is false, and the `Transaction` record’s reason matches the expectations stated in the doc (e.g., “Settlement retur”).

## 4. Simpan Sebagai DP (Per `docs/purchase_settlement_logic.md:104-130`)
1. “Phase A” conditions (source paid, target unpaid/partial) currently live in UI/controller elsewhere, but the backend must enforce them before credit creation.  The `CREDIT` case in `applySettlementEffect` (`Modules/...Controller.php:614-679`) already creates/updates a `SupplierCredit` (`firstOrCreate` at lines 616-629).  Confirm this happens only when the supplier’s paid purchase is fully resolved and that there is another purchase with status `Unpaid`/`Partial` before letting users pick the credit flow.
2. For “Phase B”, step through the code that makes the `PurchasePayment` (lines 646-665) and ensures reimbursements go against the selected target purchase.  The doc requires the amount to be capped at the target’s due amount (lines 122-128); verify `itemAmount` does not exceed `purchase->due_amount` and adjust the `PurchasePayment` creation to cap or split the credit if necessary.  Also, confirm that attachments from approval are saved on the payment (`lines 656-660`) and referenced in the generated note.
3. After crediting, the code creates a `PurchasePaymentCreditApplication` (`lines 667-673`) and decrements the `SupplierCredit` remaining amount (`lines 674-678`).  Add post-condition checks so that when `remaining_amount <= 0.01` the credit closes, matching the doc’s “remainder sits as open credit” rule.  If the return amount exceeds the due amount, plan for splitting the credit between the purchase target and the residual credit record.
4. The doc mandates linking the deposit to another purchase’s outstanding amount.  Use the existing `supplierCredit` relation to capture that link and document any UI fields (e.g., target purchase dropdown) that need to persist the connection.  If there are no eligible purchases, fall back to a guard that prevents the user from selecting “Save as DP” (otherwise instruct the UI to hide that option, matching Phase A rules in the doc).

### Next Steps
1. Re-run any existing tests touching `PurchaseReturnSettlementController` (especially Livewire/feature tests) to ensure these flows stay green after adding archiving or credit caps.
2. Coordinate with the UI owner to keep `Kelola Penyelesaian` forms aligned with the input contracts described here, so that frontend validation matches the backend expectations documented above.
